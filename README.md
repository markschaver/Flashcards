# Flash-cards

A Claude-built no-install flashcard app for studying any subject.

Load a pipe-delimited file (`front | back`, up to 200 characters per side) or paste cards directly.

Uses browser storage to persist across sessions.